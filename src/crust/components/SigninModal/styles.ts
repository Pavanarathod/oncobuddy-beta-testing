const styles = (theme: any) => ({
  buttonContainer: {
    display: "flex",
    justifyContent: "center",
    marginTop: "20px",
    marginBottom: "20px",
  },
  loginButton: {
    padding: "10px 30px",
    backgroundColor: "#000",
    color: "#fff",
  },
});

export default styles;
